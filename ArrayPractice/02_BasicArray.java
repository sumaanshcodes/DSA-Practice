public class BasicArray {
    public static void main(String[] args) {
        int[] arr = new int[5];
        arr[0] = 10;
        arr[1] = 20;
        arr[2] = 30;
        arr[3] = 40;
        arr[4] = 50;

        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
    }

    // Array: A data structure that can hold a fixed number of values of the same type.
    //  It is used to store multiple values in a single variable, instead of declaring 
    //  separate variables for each value. Arrays are indexed, meaning that each element
    //  can be accessed using its index (starting from 0). They are commonly used for 
    // storing and manipulating collections of data efficiently.
    //
    // Example of an array declaration and initialization:
    // int[] arr = new int[5]; // Declares an array of integers with a size of 5
    // arr[0] = 10; // Assigns the value 10 to the first element of the array
    // arr[1] = 20; // Assigns the value 20 to the second element of the array
    // arr[2] = 30; // Assigns the value 30 to the third element of the array
    // arr[3] = 40; // Assigns the value 40 to the fourth element of the array
    // arr[4] = 50; // Assigns the value 50 to the fifth element of the array   

    // Example of an array declaration and initialization using an array literal:
    // int[] arr = {10, 20, 30, 40, 50}; // Declares and initializes an array of integers
    // with the specified values
    // Example of accessing array elements:
    // int firstElement = arr[0]; // Accesses the first element of the array
    // int secondElement = arr[1]; // Accesses the second element of the array  
        

}
